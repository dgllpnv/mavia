node_modules/
dist/
build/
.next/
.expo/
coverage/
*.log
.env
.env.*
!.env.example
.turbo/
.DS_Store
*.tsbuildinfo
/tmp/

# Segredos de produção, gerados na VPS pelo `implantar.sh`. Perder este arquivo
# com o volume do Postgres vivo torna o banco inacessível — as senhas estão
# dentro dele, e só dentro dele.
infra/producao/.env
